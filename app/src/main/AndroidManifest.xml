<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="roomcontent.com.roomdbsample">

    <permission android:name="roomcontent.com.roomdbsample.com.provider.READ_WRITE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--<provider-->
            <!--android:name="roomcontent.com.roomdbsample.com.provider.StubProvider"-->
            <!--android:authorities="roomcontent.com.roomdbsample.com.provider"-->
            <!--android:exported="true"-->
            <!--android:syncable="true"/>-->

        <provider
            android:name=".com.provider.StubProvider"
            android:authorities="roomcontent.com.roomdbsample.com.provider"
            android:exported="true"
            android:protectionLevel="normal"
        />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>